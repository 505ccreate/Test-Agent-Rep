t); }
    .mode .mdesc { font-size: 11.5px; color: var(--dim); margin-top: 5px; line-height: 1.4; }

    .bands {
      display: grid; grid-template-columns: repeat(5, 1fr); gap: 8px; margin: 12px 0 0;
    }
    .band {
      border: 1px solid var(--line); border-radius: 17px; padding: 12px 8px 11px;
      text-align: center; cursor: pointer; color: var(--text);
      background: rgba(255,255,255,.62); transition: transform .18s, border-color .25s, background .25s;
    }
    .band:active { transform: scale(.97); }
    .band.active { border-color: var(--accent); background: var(--accent-soft); }
    .band .name { font-size: 11.5px; font-weight: 900; letter-spacing: .12em; text-transform: uppercase; }
    .band .range { font-size: 10px; color: var(--dim); margin-top: 4px; }
    .band.active .name { color: var(--accent); }

    .dashboard {
      display: grid; grid-template-columns: 1.15fr .85fr; gap: 14px; margin-top: 14px;
      align-items: start;
    }
    .stack { display: grid; gap: 14px; }
    .panel {
      background: var(--panel); border: 1px solid var(--line); border-radius: var(--radius-lg);
      padding: 22px; box-shadow: 0 12px 38px rgba(70,54,104,.075);
      backdrop-filter: blur(18px);
    }
    .panel h2 {
      margin: 0 0 16px; font-size: 11px; letter-spacing: .24em; text-transform: uppercase;
      color: #615978; font-weight: 900;
    }
    .panel h3 {
      margin: 5px 0 10px; font-size: 10px; letter-spacing: .16em; text-transform: uppercase;
      color: var(--dim); font-weight: 850;
    }
    .ctl + .ctl { margin-top: 19px; }
    .ctl label {
      display: flex; justify-content: space-between; gap: 12px; margin-bottom: 11px;
      font-size: 10.5px; letter-spacing: .12em; text-transform: uppercase; color: var(--dim); font-weight: 800;
    }
    .ctl label output { color: var(--text); font-variant-numeric: tabular-nums; letter-spacing: 0; text-transform: none; }
    input[type=range] {
      width: 100%; -webkit-appearance: none; appearance: none; height: 6px; border-radius: 99px;
      background: linear-gradient(90deg, var(--accent-soft), rgba(95,76,132,.13)); outline: none;
    }
    input[type=range]::-webkit-slider-thumb {
      -webkit-appearance: none; width: 25px; height: 25px; border-radius: 50%;
      background: var(--accent); border: 5px solid #fff; box-shadow: 0 4px 14px rgba(65,50,95,.25);
      cursor: pointer; transition: background .7s ease;
    }
    input[type=range]::-moz-range-thumb {
      width: 17px; height: 17px; border-radius: 50%; background: var(--accent);
      border: 5px solid #fff; box-shadow: 0 4px 14px rgba(65,50,95,.25); cursor: pointer;
    }
    .carrier-row { display: flex; gap: 8px; align-items: center; }
    .carrier-row input[type=range] { flex: 1; }
    .chip {
      border: 1px solid var(--line); border-radius: 999px; color: var(--dim);
      background: rgba(255,255,255,.75); padding: 7px 10px; cursor: pointer; font-size: 10.5px; font-weight: 800;
    }
    .chip.on { color: var(--accent); border-color: var(--accent); background: var(--accent-soft); }

    .tune-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 8px; }
    .tune {
      border: 1px solid var(--line); border-radius: 16px; padding: 12px 13px; cursor: pointer;
      text-align: left; color: var(--text); background: rgba(255,255,255,.58);
      transition: transform .18s, border-color .25s, background .25s;
    }
    .tune:active { transform: scale(.98); }
    .tune.on { border-color: var(--accent); background: var(--accent-soft); }
    .tune .tname { font-size: 12.5px; font-weight: 850; }
    .tune.on .tname { color: var(--accent); }
    .tune .tspec { font-size: 10.5px; color: var(--dim); margin-top: 4px; line-height: 1.35; }
    .evidence-note {
      margin-top: 14px; border-radius: 15px; padding: 13px 14px; background: rgba(116,96,154,.07);
      font-size: 10.5px; color: var(--dim); line-height: 1.55;
    }

    .bath-head { display: flex; justify-content: space-between; align-items: center; gap: 12px; }
    #bathToggle {
      border: 1px solid var(--line); border-radius: 999px; color: var(--dim); background: rgba(255,255,255,.7);
      font-size: 11px; font-weight: 900; padding: 7px 15px; cursor: pointer;
    }
    #bathToggle.on { color: var(--accent); border-color: var(--accent); background: var(--accent-soft); }
    .bath-desc { font-size: 11.5px; color: var(--dim); line-height: 1.58; }
    .bath-desc b { color: var(--text); }
    .bath-ctls { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; margin-top: 18px; }

    .scapes { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; }
    .scape {
      border: 1px solid var(--line); border-radius: 16px; padding: 12px 11px; cursor: pointer;
      background: rgba(255,255,255,.58); transition: transform .18s, border-color .25s, background .25s;
    }
    .scape:active { transform: scale(.98); }
    .scape.on { border-color: var(--accent); background: var(--accent-soft); }
    .scape .sname { font-size: 12.5px; font-weight: 850; }
    .scape.on .sname { color: var(--accent); }
    .scape .sdesc { font-size: 10.5px; color: var(--dim); margin-top: 3px; }
    .scape input[type=range] { margin-top: 11px; height: 4px; }

    .play-dock {
      position: fixed; z-index: 10; left: 50%; bottom: max(18px, env(safe-area-inset-bottom));
      transform: translateX(-50%); width: min(720px, calc(100% - 28px));
      display: flex; align-items: center; gap: 15px; padding: 12px 15px 12px 12px;
      border-radius: 25px; background: rgba(255,255,255,.91); border: 1px solid rgba(75,58,106,.14);
      box-shadow: 0 20px 70px rgba(69,51,101,.22); backdrop-filter: blur(22px);
    }
    #playBtn {
      width: 66px; height: 66px; flex: 0 0 66px; border-radius: 22px;
      border: 0; background: linear-gradient(145deg, var(--accent), color-mix(in srgb, var(--accent), #7257c7 28%));
      color: white; font-size: 23px; cursor: pointer; display: grid; place-items: center;
      box-shadow: 0 1