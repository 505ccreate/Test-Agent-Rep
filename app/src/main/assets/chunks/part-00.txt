<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#f7f2ff">
  <meta name="color-scheme" content="light">
  <meta name="mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="default">
  <meta name="description" content="A private, offline binaural, isochronic and spatial rain sound studio.">
  <title>Wavelength IV</title>
  <link rel="manifest" href="./manifest.webmanifest">
  <link rel="icon" type="image/svg+xml" href="./favicon.svg">
  <style>
    :root {
      --bg: #f7f2ff;
      --bg-deep: #eee5fb;
      --panel: rgba(255,255,255,.84);
      --panel-solid: #fffdfb;
      --line: rgba(70,54,104,.14);
      --text: #302a45;
      --dim: #746c8d;
      --accent: #e49a6f;
      --accent-soft: rgba(228,154,111,.14);
      --shadow: 0 18px 55px rgba(74,55,108,.12);
      --radius-xl: 28px;
      --radius-lg: 22px;
      --radius-md: 16px;
    }
    * { box-sizing: border-box; }
    html { min-height: 100%; background: var(--bg); }
    body {
      margin: 0;
      min-height: 100vh;
      color: var(--text);
      font-family: Inter, ui-rounded, "SF Pro Rounded", "Segoe UI", system-ui, -apple-system, sans-serif;
      background:
        radial-gradient(circle at 8% 7%, rgba(255,169,193,.28), transparent 30rem),
        radial-gradient(circle at 92% 12%, rgba(105,201,189,.25), transparent 34rem),
        radial-gradient(circle at 48% 100%, rgba(147,118,216,.18), transparent 40rem),
        linear-gradient(155deg, #fffaf5 0%, #f8f1ff 52%, #eef9f8 100%);
      padding: max(18px, env(safe-area-inset-top)) max(18px, env(safe-area-inset-right))
               max(120px, calc(38px + env(safe-area-inset-bottom))) max(18px, env(safe-area-inset-left));
      overflow-x: hidden;
    }
    button, input { font: inherit; }
    button { -webkit-tap-highlight-color: transparent; }
    .app-shell { width: min(1180px, 100%); margin: 0 auto; position: relative; }
    .topbar {
      display: flex; align-items: center; justify-content: space-between; gap: 20px;
      margin: 4px 0 18px;
    }
    .brand { display: flex; align-items: center; gap: 14px; min-width: 0; }
    .brand-mark {
      width: 58px; height: 58px; flex: 0 0 58px; display: grid; place-items: center;
      border-radius: 19px; background: rgba(255,255,255,.84);
      border: 1px solid rgba(101,77,142,.12); box-shadow: 0 12px 30px rgba(102,77,143,.13);
    }
    .brand-mark svg { width: 46px; height: 46px; }
    .brand-copy h1 {
      margin: 0; font-family: Georgia, "Times New Roman", serif; font-size: clamp(24px, 3vw, 38px);
      line-height: .95; letter-spacing: -.035em; font-weight: 600;
    }
    .brand-copy p {
      margin: 7px 0 0; color: var(--dim); font-size: 12px; letter-spacing: .15em; text-transform: uppercase;
    }
    .top-actions { display: flex; align-items: center; gap: 10px; }
    .session-pill, .install-btn {
      border: 1px solid var(--line); background: rgba(255,255,255,.72); color: var(--text);
      border-radius: 999px; padding: 10px 14px; box-shadow: 0 8px 20px rgba(70,54,104,.07);
      font-size: 12px; font-weight: 700;
    }
    .session-pill span { color: var(--accent); font-variant-numeric: tabular-nums; margin-left: 5px; }
    .install-btn { cursor: pointer; color: #6b4bb1; }
    .install-btn[hidden] { display: none; }

    .hero-card {
      position: relative; overflow: hidden; border-radius: var(--radius-xl);
      background: linear-gradient(135deg, rgba(255,255,255,.94), rgba(255,255,255,.65));
      border: 1px solid rgba(80,58,121,.13); box-shadow: var(--shadow);
      min-height: 244px;
    }
    .hero-card::before {
      content: ""; position: absolute; inset: -50% -10% auto 25%; height: 300px;
      background: radial-gradient(circle, var(--accent-soft), transparent 68%);
      pointer-events: none; transition: background .7s ease;
    }
    #scope { display: block; width: 100%; height: 244px; }
    .readout {
      position: absolute; inset: 30px auto auto 34px; pointer-events: none;
    }
    .readout .eyebrow {
      color: var(--dim); font-size: 10px; font-weight: 800; letter-spacing: .24em; text-transform: uppercase;
      margin-bottom: 7px;
    }
    .readout .big {
      font-size: clamp(42px, 7vw, 74px); line-height: .95; font-weight: 300; color: var(--accent);
      font-variant-numeric: tabular-nums; letter-spacing: -.05em; transition: color .7s ease;
    }
    .readout .big span:last-child { font-size: 17px; color: var(--dim); margin-left: 8px; letter-spacing: 0; }
    .readout .state {
      font-size: 11px; text-transform: uppercase; letter-spacing: .18em; color: var(--dim); margin-top: 10px;
    }
    .headphones {
      position: absolute; right: 26px; top: 24px; font-size: 11px; color: var(--dim);
      letter-spacing: .08em; background: rgba(255,255,255,.64); border: 1px solid var(--line);
      padding: 9px 12px; border-radius: 999px; backdrop-filter: blur(10px);
    }
    .headphones b { color: var(--accent); transition: color .7s ease; }

    .mode {
      display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-top: 12px;
    }
    .mode button {
      min-height: 76px; border: 1px solid var(--line); border-radius: 19px;
      color: var(--text); padding: 15px 17px; cursor: pointer; text-align: left;
      background: rgba(255,255,255,.67); box-shadow: 0 8px 24px rgba(70,54,104,.05);
      transition: transform .18s ease, border-color .25s, background .25s, box-shadow .25s;
    }
    .mode button:active { transform: scale(.985); }
    .mode button.on {
      border-color: var(--accent); background: linear-gradient(135deg, rgba(255,255,255,.92), var(--accent-soft));
      box-shadow: 0 12px 30px var(--accent-soft);
    }
    .mode .mname { font-size: 15px; font-weight: 800; }
    .mode button.on .mname { color: var(--accen